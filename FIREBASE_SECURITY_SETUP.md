# Firebase 보안 설정 안내 (관리자만 확인)

메일로 계속 오던 "실시간 데이터베이스에 안전하지 않은 규칙이 있습니다" 경고를
해결하기 위한 변경입니다. 코드는 이미 수정해서 푸시했고, **아래 2가지는
Firebase 콘솔에서 직접 해주셔야** 적용됩니다 (Claude가 대신 로그인할 수 없는 부분).

## 1. 관리자 로그인 계정 만들기 (Authentication)

기존에는 비밀번호 `1120`이 코드에 그대로 노출되어 있어서, 사이트 소스만 보면
누구나 관리자 기능(테마·문구·장소·갤러리 변경, 방명록 삭제)을 쓸 수 있었습니다.
이제는 진짜 로그인 계정으로 바뀌었습니다.

1. https://console.firebase.google.com → 프로젝트 `first-party-756ff` 선택
2. 왼쪽 메뉴 **Authentication** → **Sign-in method** 탭 → **이메일/비밀번호** 제공업체 **사용 설정**
3. **Users** 탭 → **사용자 추가** 를 2번 눌러 아래 두 계정을 만듭니다.
   (이메일 주소는 실제로 받을 필요 없는 로그인 전용 아이디입니다. 원하면 다른 걸로 바꿔도 되는데,
   바꾸면 `index.html` 의 `ADMIN_LOGIN_EMAIL` 부분도 같이 바꿔야 합니다.)

   | 용도 | 이메일 | 비밀번호 |
   |------|--------|----------|
   | 아빠 | `father@jiyu-first-party.app` | 원하는 비밀번호로 지정 |
   | 엄마 | `mother@jiyu-first-party.app` | 원하는 비밀번호로 지정 |

4. 사이트에서 관리자(⚙) 로그인 시, 드롭다운에서 아빠/엄마 선택 후 여기서 지정한 비밀번호 입력하면 됩니다.

## 2. 보안 규칙 적용 (Realtime Database Rules)

이 저장소의 `database.rules.json` 내용을 그대로 콘솔에 붙여넣으면 됩니다.

1. Firebase 콘솔 → **Realtime Database** → **규칙(Rules)** 탭
2. 현재 규칙(아마 `{"rules": {".read": true, ".write": true}}` 형태일 것)을
   이 저장소의 `database.rules.json` 내용으로 전체 교체
3. **게시(Publish)** 클릭

### 이 규칙이 하는 일
- **읽기는 계속 전체 공개** — 돌잔치 초대 페이지라 누구나 볼 수 있어야 하므로 유지
- **`guestbook`(방명록)은 로그인한 사용자만 쓰기 가능** — 행사가 끝나 손님이 새로 글을 남길 필요는 없어졌지만, 스팸/부적절한 글 삭제나 오탈자 수정 등 관리 목적으로 아빠/엄마 계정 로그인 시에는 계속 쓰기(수정/삭제)가 가능하도록 열어둠.
- **`settings`, `texts`, `location`, `gallery`(테마·문구·장소·갤러리 등 관리자 콘텐츠)는 로그인한 사용자만 쓰기 가능** — 이제 이 부분이 실제로 서버(Firebase Rules) 단에서 막힙니다. 이전에는 "관리자 로그인" 화면이 있어도 실제로는 아무나 브라우저 개발자도구로 직접 값을 바꿀 수 있었는데, 이제는 진짜 로그인해야만 가능합니다.

## 확인 방법
설정 완료 후:
- 로그아웃 상태에서 개발자도구 콘솔에 `db.ref('settings/theme').set('x')` 같은 걸 시도하면 `PERMISSION_DENIED` 가 떠야 정상입니다.
- 사이트에서 아빠/엄마로 로그인하면 테마 변경 등 관리자 기능이 정상 동작해야 합니다.
- Firebase 콘솔의 "규칙 검사" 도구로도 확인 가능합니다.

적용 후에는 "안전하지 않은 규칙" 경고 메일이 더 이상 오지 않습니다.
